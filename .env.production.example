# Production environment template (Clerk live keys, production domain).

# Database connection (replace with your managed Postgres URL or Render secret).
DATABASE_URL=postgresql://user:password@db-host:5432/personal-planner

# URLs for your deployed site (no trailing slash).
APP_URL=https://veluna-labs-planer.de
NEXT_PUBLIC_APP_URL=https://veluna-labs-planer.de
NEXTAUTH_URL=https://veluna-labs-planer.de

# Authentication (Clerk) — use the live keys from your production instance.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_xxx 
CLERK_SECRET_KEY=sk_live_xxx

# Google Calendar OAuth (optional).
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
# Legacy fallbacks still supported:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# NextAuth secret for session encryption (replace with 32+ random chars).
NEXTAUTH_SECRET=change-me

# Optional extras (notifications / telemetry, leave empty if unused).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
